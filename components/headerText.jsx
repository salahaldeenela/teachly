import { View, Text } from 'react-native';
import React from 'react';

const HeaderText = ({ text }) => {
  return <Text className="text-2xl font-exoSemibold">{text}</Text>;
};

export default HeaderText;
